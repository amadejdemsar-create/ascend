---
phase: 09-gamification-and-recurring-goals
plan: 3
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - lib/services/dashboard-service.ts
  - components/ui/xp-progress-bar.tsx
  - components/dashboard/streaks-stats-widget.tsx
  - components/dashboard/dashboard-page.tsx
  - lib/hooks/use-dashboard.ts
autonomous: true
requirements:
  - GAME-04
  - GAME-07

must_haves:
  truths:
    - "Dashboard displays current level and XP progress to next level as an animated bar"
    - "Dashboard shows active streaks count from recurring goals"
    - "Dashboard shows weekly score that resets on Monday boundary"
    - "Stats widget includes level, XP bar, streak count, and weekly score alongside existing stats"
  artifacts:
    - path: "components/ui/xp-progress-bar.tsx"
      provides: "Animated XP progress bar component with level label"
    - path: "components/dashboard/streaks-stats-widget.tsx"
      provides: "Enhanced stats widget with gamification data"
    - path: "lib/services/dashboard-service.ts"
      provides: "Extended StatsData interface with weeklyScore, longestStreak, xpToNext, activeStreaks"
  key_links:
    - from: "components/dashboard/streaks-stats-widget.tsx"
      to: "lib/services/dashboard-service.ts"
      via: "StatsData interface with gamification fields"
      pattern: "StatsData"
    - from: "components/dashboard/streaks-stats-widget.tsx"
      to: "components/ui/xp-progress-bar.tsx"
      via: "XpProgressBar component usage"
      pattern: "XpProgressBar"
    - from: "components/dashboard/dashboard-page.tsx"
      to: "app/api/goals/recurring/generate/route.ts"
      via: "useDashboard hook triggers recurring instance generation on load"
      pattern: "recurring/generate"
---

<objective>
Extend the dashboard to display gamification data: XP progress bar with level, active streaks, weekly score, and trigger recurring instance generation on dashboard load.

Purpose: The dashboard is where the user gets their daily motivation snapshot. Showing XP progress, level, streaks, and weekly score transforms it from a task list into a gamified goal tracker. Triggering recurring instance generation on dashboard load ensures users always see their due recurring goals without manual intervention or cron infrastructure.
Output: XP progress bar component, enhanced stats widget, dashboard integration with recurring generation.
</objective>

<execution_context>
@/Users/amadejdemsar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/amadejdemsar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-gamification-and-recurring-goals/09-RESEARCH.md
@.planning/phases/09-gamification-and-recurring-goals/09-01-SUMMARY.md

<interfaces>
<!-- From existing codebase and Plan 01 outputs -->

From lib/services/dashboard-service.ts (current StatsData):
```typescript
export interface StatsData {
  completedThisMonth: number;
  totalGoals: number;
  totalCompleted: number;
  completionRate: number;
  currentXp: number;
  level: number;
  currentStreak: number;
}
```

From lib/constants.ts (Plan 01 additions):
```typescript
export function xpForLevel(level: number): number;
export function levelFromXp(totalXp: number): number;
export function xpToNextLevel(totalXp: number): { current: number; needed: number; percentage: number };
```

From lib/services/gamification-service.ts (Plan 01):
```typescript
export const gamificationService = {
  async getStats(userId: string): Promise<{ totalXp, level, currentStreak, longestStreak, weeklyScore, xpToNext: { current, needed, percentage } }>;
};
```

From components/dashboard/streaks-stats-widget.tsx (current):
```typescript
// Shows: completedThisMonth, completionRate, totalGoals, totalCompleted
// Needs: level, XP bar, streak with fire icon, weekly score
```

From lib/hooks/use-dashboard.ts:
```typescript
export function useDashboard() {
  return useQuery({
    queryKey: queryKeys.dashboard(),
    queryFn: () => fetchJson<DashboardData>("/api/dashboard"),
  });
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend dashboard service with gamification data and XP bar component</name>
  <files>
    lib/services/dashboard-service.ts
    components/ui/xp-progress-bar.tsx
  </files>
  <action>
1. Update `lib/services/dashboard-service.ts`:
   - Import `xpToNextLevel` from `@/lib/constants`
   - Extend `StatsData` interface with: `weeklyScore: number`, `longestStreak: number`, `activeStreaks: number`, `xpToNext: { current: number; needed: number; percentage: number }`
   - In the `getDashboardData` method's parallel batch 2, also query active recurring streaks: `prisma.goal.count({ where: { userId, isRecurring: true, recurringSourceId: null, currentStreak: { gt: 0 } } })`
   - In the streaksStats construction, add the new fields from userStats: `weeklyScore: userStats?.weeklyScore ?? 0`, `longestStreak: userStats?.longestStreak ?? 0`, `activeStreaks: activeStreaksCount`, `xpToNext: xpToNextLevel(userStats?.totalXp ?? 0)`
   - Check and reset weekly score if weekStartDate is stale (before current Monday). Import `startOfWeek` from date-fns. If `userStats?.weekStartDate` exists and is before `startOfWeek(now, { weekStartsOn: 1 })`, set weeklyScore to 0 in the response (do not mutate DB here, gamification-service handles resets on XP award).

2. Create `components/ui/xp-progress-bar.tsx`:
   - Client component ("use client")
   - Props: `{ current: number; needed: number; percentage: number; level: number }`
   - Render a labeled progress bar: "Level {level}" on the left, "{current} / {needed} XP" on the right
   - Inner div uses `className="h-full rounded-full bg-primary transition-all duration-500 ease-in-out"` with `style={{ width: \`${percentage}%\` }}`
   - Outer div uses `className="h-2 w-full rounded-full bg-muted"` with proper `role="progressbar"` and `aria-valuenow`, `aria-valuemin`, `aria-valuemax`, `aria-label` attributes
   - Typography uses font-mono for numbers (JetBrains Mono, per project conventions)
  </action>
  <verify>
    <automated>cd /Users/Shared/Domain/Code/Personal/ascend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>StatsData includes weeklyScore, longestStreak, activeStreaks, xpToNext. XpProgressBar component renders an accessible animated bar with level label. TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Enhanced stats widget and dashboard recurring generation trigger</name>
  <files>
    components/dashboard/streaks-stats-widget.tsx
    components/dashboard/dashboard-page.tsx
    lib/hooks/use-dashboard.ts
  </files>
  <action>
1. Rewrite `components/dashboard/streaks-stats-widget.tsx`:
   - Import XpProgressBar from `@/components/ui/xp-progress-bar`
   - The card should have two sections:
     a) **Top section**: XP Progress bar (full width, showing level and XP to next level). Use `stats.xpToNext` props.
     b) **Bottom section**: Grid of stat items. Replace the current 4 items with 6 items in a responsive grid (grid-cols-2 sm:grid-cols-3):
        - Level (Trophy icon, stats.level)
        - Weekly Score (Zap icon, stats.weeklyScore, suffix "pts")
        - Active Streaks (Flame icon, stats.activeStreaks)
        - Completed This Month (CalendarCheck icon, stats.completedThisMonth)
        - Completion Rate (Percent icon, stats.completionRate, suffix "%")
        - Total Completed (CheckCircle2 icon, stats.totalCompleted)
   - Import additional icons from lucide-react: `Trophy`, `Zap`
   - Update the card title from "Stats" to "Level & Stats"
   - Ensure the stat values use `font-mono text-2xl font-bold` for consistency with existing pattern

2. Update `lib/hooks/use-dashboard.ts`:
   - Add a `useGenerateRecurring` mutation hook that POSTs to `/api/goals/recurring/generate` (fire and forget)
   - In `useDashboard`, add an `onSuccess` callback (or use `useEffect` on the query data) that calls the recurring generate endpoint once per session. Use a module-level flag `let recurringGenerated = false` to prevent re-triggering on every refetch. This ensures recurring instances are generated when the dashboard first loads.

3. Update `components/dashboard/dashboard-page.tsx`:
   - No structural changes needed since the stats widget receives data as props via useDashboard()
   - If the component needs to call the recurring generation, add it here. Otherwise, the hook handles it.
   - Verify the dashboard page passes the full StatsData to StreaksStatsWidget (it should already via `data.streaksStats`)
  </action>
  <verify>
    <automated>cd /Users/Shared/Domain/Code/Personal/ascend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Stats widget shows XP progress bar, level, weekly score, active streaks, completion stats. Dashboard triggers recurring instance generation on first load. TypeScript compiles cleanly.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- StatsData interface includes all gamification fields
- XpProgressBar component exists with proper accessibility attributes
- Stats widget renders XP bar at top and 6 stat items below
- Dashboard hook triggers recurring generation on first load
- Weekly score displays 0 if the week has reset
</verification>

<success_criteria>
1. Dashboard shows "Level {N}" with an XP progress bar showing progress to next level
2. Stats widget displays weekly score, active streaks count, and all existing stats
3. Recurring instance generation triggers automatically on dashboard load (once per session)
4. XP bar animates smoothly when progress changes (CSS transition)
5. All stat values use monospace font for data consistency
</success_criteria>

<output>
After completion, create `.planning/phases/09-gamification-and-recurring-goals/09-03-SUMMARY.md`
</output>
