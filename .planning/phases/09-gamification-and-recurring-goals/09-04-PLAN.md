---
phase: 09-gamification-and-recurring-goals
plan: 4
type: execute
wave: 3
depends_on:
  - 09-01
  - 09-03
files_modified:
  - lib/hooks/use-celebrations.ts
  - components/goals/goal-status-select.tsx
  - components/goals/goal-card.tsx
  - components/goals/goal-board-card.tsx
  - components/goals/goal-list-columns.tsx
  - components/goals/goal-tree-node.tsx
  - components/goals/goal-timeline-node.tsx
  - components/goals/goal-drag-overlay.tsx
  - components/dashboard/weekly-focus-widget.tsx
  - package.json
autonomous: true
requirements:
  - GAME-05
  - GAME-06

must_haves:
  truths:
    - "Completing a YEARLY or QUARTERLY goal fires a confetti burst animation"
    - "Completing a MONTHLY or WEEKLY goal plays a subtle CSS checkmark animation"
    - "Leveling up triggers a multi-burst fireworks celebration"
    - "Progress bars across all views animate smoothly when their width changes"
    - "All animations respect the prefers-reduced-motion system setting"
  artifacts:
    - path: "lib/hooks/use-celebrations.ts"
      provides: "Client hook for triggering confetti and level-up animations"
      exports: ["useCelebrations"]
    - path: "package.json"
      provides: "canvas-confetti dependency installed"
      contains: "canvas-confetti"
  key_links:
    - from: "components/goals/goal-status-select.tsx"
      to: "lib/hooks/use-celebrations.ts"
      via: "celebrateGoalComplete and celebrateLevelUp calls after status change"
      pattern: "celebrateGoalComplete|celebrateLevelUp"
    - from: "lib/hooks/use-celebrations.ts"
      to: "canvas-confetti"
      via: "confetti() function calls"
      pattern: "import confetti"
---

<objective>
Add celebration animations on goal completion and level-up, and add smooth CSS transitions to all progress bars across the application.

Purpose: Satisfying visual feedback is what transforms a tracking tool into something users want to come back to. Confetti on milestones, a checkmark animation on tasks, fireworks on level-up, and smooth progress bar transitions create a sense of accomplishment that reinforces consistency.
Output: canvas-confetti installed, celebration hook, updated status select component, animated progress bars in 7 components.
</objective>

<execution_context>
@/Users/amadejdemsar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/amadejdemsar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-gamification-and-recurring-goals/09-RESEARCH.md
@.planning/phases/09-gamification-and-recurring-goals/09-01-SUMMARY.md

<interfaces>
<!-- From existing codebase and Plan 01 outputs -->

From components/goals/goal-status-select.tsx:
```typescript
// handleChange calls useUpdateGoal which fetches PATCH /api/goals/[id]
// Response now includes _xp: { amount, totalXp, level, leveledUp } when completing
// After mutation succeeds, needs to check response for _xp and trigger celebrations
interface GoalStatusSelectProps {
  goalId: string;
  currentStatus: string;
  parentId?: string | null;
  onStatusChange?: (newStatus: string) => void;
}
```

From lib/hooks/use-goals.ts (useUpdateGoal):
```typescript
// mutationFn returns the full response including _xp when status changes to COMPLETED
// The mutation result is available in onSuccess callback and mutateAsync return value
```

Progress bar pattern used across 7 components (all use the same inline style):
```tsx
<div className="h-1.5 rounded-full bg-muted">
  <div
    className="h-full rounded-full bg-primary"
    style={{ width: `${Math.min(goal.progress, 100)}%` }}
  />
</div>
```
Needs: Add `transition-all duration-500 ease-in-out` to the inner div's className.

Components with progress bars:
- components/goals/goal-card.tsx (line ~74)
- components/goals/goal-board-card.tsx (line ~68)
- components/goals/goal-list-columns.tsx (line ~86)
- components/goals/goal-tree-node.tsx (line ~138)
- components/goals/goal-timeline-node.tsx (lines ~77, ~110)
- components/goals/goal-drag-overlay.tsx (line ~37)
- components/dashboard/weekly-focus-widget.tsx (line ~48)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install canvas-confetti and create celebration hook</name>
  <files>
    package.json
    lib/hooks/use-celebrations.ts
  </files>
  <action>
1. Install canvas-confetti: `cd /Users/Shared/Domain/Code/Personal/ascend && npm install canvas-confetti && npm install --save-dev @types/canvas-confetti`

2. Create `lib/hooks/use-celebrations.ts`:
   - "use client" directive
   - Import `confetti` from `canvas-confetti`
   - Export `useCelebrations()` hook returning `{ celebrateGoalComplete, celebrateLevelUp }`:

   a) `celebrateGoalComplete(horizon: string)`:
      - If `window.matchMedia("(prefers-reduced-motion: reduce)").matches`, return early (respect system setting even though canvas-confetti has its own flag, this avoids any flicker)
      - For YEARLY or QUARTERLY: fire `confetti({ particleCount: 150, spread: 80, origin: { y: 0.6 }, disableForReducedMotion: true })` and a second burst 200ms later with `confetti({ particleCount: 100, spread: 60, origin: { y: 0.65 }, disableForReducedMotion: true })`
      - For MONTHLY or WEEKLY: no confetti. Instead, return a flag `{ showCheckmark: true }` that the caller can use to trigger a CSS animation on the status indicator. The simplest implementation: the function always returns `{ showCheckmark: boolean }` where checkmark is true for MONTHLY/WEEKLY completions.

   b) `celebrateLevelUp()`:
      - If reduced motion, return early
      - Fire dual-burst fireworks: setInterval every 250ms for 3 seconds, alternating left (`origin: { x: 0 }`, `angle: 60`) and right (`origin: { x: 1 }`, `angle: 120`) bursts of 50 particles each with `spread: 55` and `disableForReducedMotion: true`. Clear interval when duration expires.

3. Keep particle counts moderate (50 per burst for tasks, 150 max for milestones) per research recommendation to avoid main thread jank on mobile.
  </action>
  <verify>
    <automated>cd /Users/Shared/Domain/Code/Personal/ascend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>canvas-confetti installed. use-celebrations.ts exports useCelebrations with celebrateGoalComplete and celebrateLevelUp. Both respect prefers-reduced-motion. TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Wire celebrations into status select and animate all progress bars</name>
  <files>
    components/goals/goal-status-select.tsx
    components/goals/goal-card.tsx
    components/goals/goal-board-card.tsx
    components/goals/goal-list-columns.tsx
    components/goals/goal-tree-node.tsx
    components/goals/goal-timeline-node.tsx
    components/goals/goal-drag-overlay.tsx
    components/dashboard/weekly-focus-widget.tsx
  </files>
  <action>
1. Update `components/goals/goal-status-select.tsx`:
   - Import `useCelebrations` from `@/lib/hooks/use-celebrations`
   - Call `const { celebrateGoalComplete, celebrateLevelUp } = useCelebrations()` inside the component
   - The component needs to know the goal's horizon to determine celebration type. Add `horizon?: string` to GoalStatusSelectProps.
   - In `handleChange`, after the successful `updateGoal.mutateAsync` call, check the response:
     - The mutation returns the API response. Type it to include optional `_xp?: { amount: number; totalXp: number; level: number; leveledUp: boolean; weeklyScore: number }`
     - If `newStatus === "COMPLETED"` and `horizon` is provided: call `celebrateGoalComplete(horizon)`
     - If the response has `_xp?.leveledUp === true`: call `celebrateLevelUp()` (fires after a brief setTimeout of 500ms to let the completion confetti play first)
   - For the checkmark animation on MONTHLY/WEEKLY: add a local state `const [showCheckmark, setShowCheckmark] = useState(false)`. When celebrateGoalComplete returns `{ showCheckmark: true }`, set it and clear after 1 second. Render a small animated checkmark SVG overlay (green circle with animated stroke-dashoffset check path) next to the status indicator when showCheckmark is true.
   - Update ALL usages of GoalStatusSelect across the codebase to pass the `horizon` prop. The components that use it (goal-detail, goal-card, etc.) already have access to the goal's horizon.

2. Add CSS transition to progress bars in all 7 components:
   - In each component's progress bar inner div, add `transition-all duration-500 ease-in-out` to the existing className.
   - Pattern to find: `className="h-full rounded-full bg-primary"` or similar. Change to `className="h-full rounded-full bg-primary transition-all duration-500 ease-in-out"`.
   - Do this in:
     a) `goal-card.tsx` (the progress bar div)
     b) `goal-board-card.tsx`
     c) `goal-list-columns.tsx`
     d) `goal-tree-node.tsx`
     e) `goal-timeline-node.tsx` (two progress bars)
     f) `goal-drag-overlay.tsx`
     g) `weekly-focus-widget.tsx`
   - This is a simple class addition. No structural changes needed.
  </action>
  <verify>
    <automated>cd /Users/Shared/Domain/Code/Personal/ascend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>GoalStatusSelect triggers confetti on YEARLY/QUARTERLY completion, checkmark on MONTHLY/WEEKLY, fireworks on level-up. All 7 progress bar components have smooth CSS transitions. Animations respect prefers-reduced-motion. TypeScript compiles cleanly.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- canvas-confetti appears in package.json dependencies
- @types/canvas-confetti appears in devDependencies
- use-celebrations.ts exports useCelebrations hook
- GoalStatusSelect accepts horizon prop and triggers appropriate celebration
- All 7 progress bar components include `transition-all duration-500 ease-in-out` class
- prefers-reduced-motion check exists in celebration hook
</verification>

<success_criteria>
1. Completing a YEARLY or QUARTERLY goal fires visible confetti particles
2. Completing a MONTHLY or WEEKLY goal shows a CSS checkmark animation
3. Leveling up fires a 3 second dual-burst fireworks effect
4. Scrolling through goals shows progress bars animate smoothly when data changes
5. Setting system to "reduce motion" prevents all confetti and animation effects
</success_criteria>

<output>
After completion, create `.planning/phases/09-gamification-and-recurring-goals/09-04-SUMMARY.md`
</output>
