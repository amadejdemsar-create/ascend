---
phase: 13-todo-ui
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - app/(app)/todos/page.tsx
  - components/todos/todo-list-columns.tsx
  - components/todos/todo-list-view.tsx
  - components/todos/todo-detail.tsx
  - components/todos/todo-bulk-bar.tsx
  - components/todos/todo-overdue-actions.tsx
autonomous: true
requirements: [TODO-12, TODO-13]

must_haves:
  truths:
    - "User can select multiple to-dos using checkboxes and see a count of selected items"
    - "User can bulk-complete selected to-dos with one button click"
    - "User can bulk-delete selected to-dos with one button click (with confirmation)"
    - "Overdue to-dos (dueDate in the past and status PENDING) are visually highlighted in red/orange"
    - "Overdue to-dos show inline reschedule (to today or tomorrow) and complete actions"
    - "User can view to-do details in a side panel by clicking a row"
  artifacts:
    - path: "components/todos/todo-bulk-bar.tsx"
      provides: "Floating bulk action bar with complete and delete buttons"
      exports: ["TodoBulkBar"]
    - path: "components/todos/todo-overdue-actions.tsx"
      provides: "Inline dropdown for rescheduling or completing overdue to-dos"
      exports: ["TodoOverdueActions"]
    - path: "components/todos/todo-detail.tsx"
      provides: "Side panel showing full to-do details with edit and complete actions"
      exports: ["TodoDetail"]
    - path: "components/todos/todo-list-view.tsx"
      provides: "Updated list view with checkbox column and overdue row styling"
    - path: "components/todos/todo-list-columns.tsx"
      provides: "Updated columns with checkbox column and overdue date styling"
    - path: "app/(app)/todos/page.tsx"
      provides: "Updated page with selection state, bulk bar, and detail panel"
  key_links:
    - from: "components/todos/todo-bulk-bar.tsx"
      to: "/api/todos/bulk-complete"
      via: "useBulkCompleteTodos mutation hook"
      pattern: "useBulkCompleteTodos"
    - from: "components/todos/todo-overdue-actions.tsx"
      to: "/api/todos/[id]"
      via: "useUpdateTodo and useCompleteTodo hooks"
      pattern: "useUpdateTodo|useCompleteTodo"
    - from: "components/todos/todo-detail.tsx"
      to: "/api/todos/[id]"
      via: "useTodo query hook"
      pattern: "useTodo"
---

<objective>
Add bulk actions (complete/delete), overdue visual indicators with reschedule options, and a to-do detail side panel to the to-do list page.

Purpose: Users need to efficiently manage multiple to-dos at once and immediately see which to-dos are overdue so they can act on them. The detail panel provides quick access to full to-do information without leaving the list.
Output: Checkbox selection with floating bulk action bar, red/orange overdue highlighting with reschedule dropdown, and a detail side panel matching the goals detail pattern.
</objective>

<execution_context>
@/Users/amadejdemsar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/amadejdemsar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-todo-ui/13-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Plan 01 that this plan builds on -->

From components/todos/todo-list-columns.tsx (created in 13-01):
```typescript
export interface TodoListItem {
  id: string;
  title: string;
  description: string | null;
  status: "PENDING" | "DONE" | "SKIPPED";
  priority: "LOW" | "MEDIUM" | "HIGH";
  dueDate: string | null;
  scheduledDate: string | null;
  isBig3: boolean;
  isRecurring: boolean;
  currentStreak: number;
  completedAt: string | null;
  createdAt: string;
  goal: { id: string; title: string } | null;
  category: { id: string; name: string; color: string; icon: string | null } | null;
}
export const columns: ColumnDef<TodoListItem>[];
```

From lib/hooks/use-todos.ts:
```typescript
export function useTodo(id: string): UseQueryResult
export function useCompleteTodo(): UseMutationResult
export function useUpdateTodo(): UseMutationResult
export function useDeleteTodo(): UseMutationResult
export function useBulkCompleteTodos(): UseMutationResult
```

From app/(app)/todos/page.tsx (created in 13-01):
- Page has filters state, selectedTodoId state, useTodos query
- Left panel with header, filter bar, quick-add, TodoListView
- Right panel placeholder ("Select a to-do to see details")
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bulk action bar, overdue actions, and updated columns with checkboxes</name>
  <files>
    components/todos/todo-bulk-bar.tsx
    components/todos/todo-overdue-actions.tsx
    components/todos/todo-list-columns.tsx
    components/todos/todo-list-view.tsx
  </files>
  <action>
1. Create components/todos/todo-bulk-bar.tsx:
   - A floating bar that appears at the bottom of the list when items are selected
   - Props: `selectedIds: string[]`, `onClearSelection: () => void`, `onBulkComplete: () => void`, `onBulkDelete: () => void`
   - Layout: fixed bottom bar (above mobile tab bar) with rounded-lg, shadow-lg, bg-background border
   - Shows: "{N} selected" text, "Complete" button (CheckSquare icon, calls onBulkComplete), "Delete" button (Trash2 icon, destructive variant)
   - Delete button opens an AlertDialog (from components/ui/alert-dialog.tsx) confirming "Delete {N} to-dos? This cannot be undone." with Cancel and Delete buttons
   - "Clear" button (X icon) calls onClearSelection
   - Animate in/out with a simple transition (opacity + translateY)

2. Create components/todos/todo-overdue-actions.tsx:
   - A small inline component for overdue to-dos (dueDate in the past, status PENDING)
   - Props: `todoId: string`, `dueDate: string`
   - Renders a DropdownMenu (from components/ui/dropdown-menu.tsx) with trigger being a small clock/alert icon in orange/red
   - Dropdown items:
     a. "Reschedule to today" (calls useUpdateTodo with dueDate set to today's ISO string)
     b. "Reschedule to tomorrow" (calls useUpdateTodo with dueDate set to tomorrow's ISO string)
     c. Separator
     d. "Complete now" (calls useCompleteTodo)
   - Show toast on success for each action

3. Update components/todos/todo-list-columns.tsx:
   - Add a checkbox column as the FIRST column (before title):
     - Header: a Checkbox that toggles all rows (use meta.toggleAll / meta.allSelected passed from parent)
     - Cell: a Checkbox for individual row selection (use meta.isSelected(row.original.id) / meta.toggleRow(row.original.id))
     - Import Checkbox from a simple inline implementation or use an input[type=checkbox] styled with Tailwind (the project does not have a Checkbox component in ui/, so create a minimal styled checkbox inline or use a raw HTML checkbox with `className="size-4 rounded border-border"`)
   - Update the Due Date column cell: if the to-do is overdue (dueDate < now AND status === "PENDING"), render the date in text-destructive (red) and append a TodoOverdueActions component inline

4. Update components/todos/todo-list-view.tsx:
   - Add `selectedIds: Set<string>` and `onToggleSelect: (id: string) => void`, `onToggleAll: () => void`, `allSelected: boolean` to props
   - Pass selection functions to the table via `meta` option on useReactTable:
     ```
     meta: {
       isSelected: (id: string) => selectedIds.has(id),
       toggleRow: onToggleSelect,
       toggleAll: onToggleAll,
       allSelected,
     }
     ```
   - Add overdue row styling: if a todo row has dueDate < now and status === "PENDING", add `bg-destructive/5 border-l-2 border-l-destructive` to the TableRow className
  </action>
  <verify>
    <automated>cd /Users/Shared/Domain/Code/Personal/ascend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Checkbox column renders in each row and header. Overdue to-dos show red date text with action dropdown. Bulk bar component exists with complete/delete/clear buttons and delete confirmation dialog.</done>
</task>

<task type="auto">
  <name>Task 2: Todo detail panel and page integration</name>
  <files>
    components/todos/todo-detail.tsx
    app/(app)/todos/page.tsx
  </files>
  <action>
1. Create components/todos/todo-detail.tsx following components/goals/goal-detail.tsx pattern:
   - Props: `todoId: string`, `onClose: () => void`, `isMobileOverlay?: boolean`
   - Fetches full to-do via useTodo(todoId) hook
   - Layout (scrollable panel):
     a. Header: close button (X icon), title, priority badge, Big 3 star indicator
     b. Status section: current status badge, "Complete" button (if PENDING), "Skip" button (if PENDING)
     c. Details section:
        - Due date (with overdue warning if applicable)
        - Category (with color dot)
        - Linked goal (with link icon, clickable if present)
        - Recurring info (if isRecurring: show currentStreak, consistencyScore as %)
     d. Description: render description text (or "No description" muted)
     e. Actions footer: "Edit" button (opens inline edit or just calls useUpdateTodo for simple field changes), "Delete" button (destructive, with confirmation dialog)
   - Complete and skip buttons use useCompleteTodo and useSkipTodo hooks, show toast on success
   - Delete uses useDeleteTodo hook, calls onClose after success

2. Update app/(app)/todos/page.tsx to integrate all Plan 02 components:
   - Add selection state: `selectedIds` as Set<string> (useState), toggle functions
   - Wire TodoListView with selection props (selectedIds, onToggleSelect, onToggleAll, allSelected)
   - Show TodoBulkBar when selectedIds.size > 0:
     - onBulkComplete: call useBulkCompleteTodos().mutateAsync(Array.from(selectedIds)), then clear selection
     - onBulkDelete: loop through selectedIds calling useDeleteTodo for each (the API does not have a bulk-delete endpoint, so delete sequentially with Promise.all), then clear selection
   - Replace right panel placeholder with TodoDetail:
     - Desktop: `<TodoDetail todoId={selectedTodoId} onClose={() => setSelectedTodoId(null)} />` in a 400px/440px side panel (same as goals)
     - Mobile: full-screen overlay with TodoDetail (same pattern as goals)
     - Empty state: "Select a to-do to see details" when no selection
   - Clear selectedTodoId from selection set when it is deleted
   - Clear bulk selection when filters change
  </action>
  <verify>
    <automated>cd /Users/Shared/Domain/Code/Personal/ascend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Clicking a to-do row opens detail panel showing all to-do fields with complete/skip/delete actions. Selecting multiple checkboxes shows floating bulk bar. Bulk complete and bulk delete work. Overdue to-dos are visually highlighted in red with reschedule options. Mobile shows detail as full-screen overlay.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. Checkbox in each row toggles selection, header checkbox toggles all
3. Selecting 2+ to-dos shows bulk bar at bottom
4. "Complete" in bulk bar marks all selected as done
5. "Delete" in bulk bar shows confirmation dialog, then deletes all selected
6. Overdue to-dos (past dueDate, PENDING) have red/orange left border and red date text
7. Overdue dropdown offers "Reschedule to today", "Reschedule to tomorrow", "Complete now"
8. Clicking a to-do row opens detail panel on desktop (side) and mobile (overlay)
9. Detail panel shows all fields, complete/skip/delete buttons work
</verification>

<success_criteria>
- Bulk-complete marks all selected to-dos as DONE and clears selection
- Bulk-delete removes all selected to-dos after confirmation
- Overdue to-dos have distinct visual highlighting (red/orange border + red date)
- Overdue reschedule actions update the dueDate correctly
- Detail panel loads and displays full to-do information
- All interactions show appropriate toast feedback
</success_criteria>

<output>
After completion, create `.planning/phases/13-todo-ui/13-02-SUMMARY.md`
</output>
