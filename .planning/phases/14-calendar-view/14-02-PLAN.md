---
phase: 14-calendar-view
plan: 02
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - components/calendar/morning-planning-prompt.tsx
  - app/(app)/calendar/page.tsx
autonomous: true
requirements: [CAL-07]

must_haves:
  truths:
    - "When user opens the calendar without Big 3 set for today, a morning planning prompt guides them to pick priorities"
    - "The prompt shows available to-dos for today and lets the user select up to 3 as Big 3"
    - "After setting Big 3, the prompt dismisses and the day detail shows the selected priorities"
  artifacts:
    - path: "components/calendar/morning-planning-prompt.tsx"
      provides: "Morning planning prompt overlay/inline component"
      min_lines: 50
    - path: "app/(app)/calendar/page.tsx"
      provides: "Calendar page updated with morning prompt integration"
  key_links:
    - from: "components/calendar/morning-planning-prompt.tsx"
      to: "/api/todos/big3"
      via: "useTop3Todos to check if Big 3 exist, useSetBig3 to set them"
      pattern: "useTop3Todos|useSetBig3"
    - from: "components/calendar/morning-planning-prompt.tsx"
      to: "/api/todos/by-date"
      via: "useTodosByDate to show available to-dos for selection"
      pattern: "useTodosByDate"
---

<objective>
Add the morning planning prompt that appears when the user opens the calendar without Daily Big 3 set for today, guiding them to select their top 3 priorities.

Purpose: The morning planning ritual is the daily habit that makes the calendar the primary planning surface. Without Big 3 set, the user gets a nudge to plan their day.
Output: MorningPlanningPrompt component integrated into the calendar page.
</objective>

<execution_context>
@/Users/amadejdemsar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/amadejdemsar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-calendar-view/14-01-SUMMARY.md

<interfaces>
<!-- Hooks available from Phase 12 data layer -->

From lib/hooks/use-todos.ts:
```typescript
export function useTodosByDate(date: string): UseQueryResult<TodoListItem[]>
export function useTop3Todos(date?: string): UseQueryResult<TodoListItem[]>
export function useSetBig3(): UseMutationResult  // { todoIds: string[]; date?: string }
```

From components/todos/todo-list-columns.tsx:
```typescript
export interface TodoListItem {
  id: string;
  title: string;
  status: "PENDING" | "DONE" | "SKIPPED";
  priority: "LOW" | "MEDIUM" | "HIGH";
  isBig3: boolean;
  isRecurring: boolean;
  goal: { id: string; title: string } | null;
  category: { id: string; name: string; color: string; icon: string | null } | null;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Morning planning prompt component</name>
  <files>
    components/calendar/morning-planning-prompt.tsx
  </files>
  <action>
Create `components/calendar/morning-planning-prompt.tsx`. This is a "use client" component.

Props:
- `todayTodos: TodoListItem[]` (all pending to-dos for today, passed from parent to avoid duplicate fetching)
- `onDismiss: () => void` (callback when user dismisses or completes selection)

Implementation:
1. Local state: `selectedIds: Set<string>` for tracking which todos the user has selected as Big 3.

2. Import `useSetBig3` from hooks. Also import `toast` from sonner, `Star`, `Sparkles`, `X` icons from lucide.

3. Render an inline banner/card (not a modal, since it should be non-blocking per research notes). Style it as a prominent card at the top of the calendar page:
   - `bg-primary/5 border border-primary/20 rounded-xl p-6`
   - Header: `Sparkles` icon + "Plan Your Day" in font-serif text-xl
   - Subtitle: "Pick up to 3 priorities for today" in text-muted-foreground

4. Below the header, list today's PENDING todos as selectable items:
   - Each item is a row with: a `Star` icon toggle (filled amber when selected, outline when not), todo title, priority badge (GoalPriorityBadge), and linked goal name if present.
   - Clicking a row toggles its selection in `selectedIds`.
   - When 3 items are already selected and user tries to select a 4th, show a toast: "You can only pick 3 priorities. Deselect one first."

5. Footer with two buttons:
   - "Set Big 3" (primary, disabled when `selectedIds.size === 0`): Calls `setBig3.mutateAsync({ todoIds: Array.from(selectedIds) })`, then calls `onDismiss()` on success. Show toast "Big 3 set for today!" on success.
   - "Skip for now" (ghost/outline): Calls `onDismiss()` without setting anything.

6. If `todayTodos` is empty (no pending todos for today), show a different message: "No to-dos for today. Create some from the to-dos page to plan your day." with a "Got it" dismiss button.
  </action>
  <verify>
    `cd /Users/Shared/Domain/Code/Personal/goals && npx tsc --noEmit` passes with zero errors
  </verify>
  <done>MorningPlanningPrompt component renders a non-blocking inline card with selectable to-dos, enforces max 3 selection, and calls useSetBig3 on confirmation</done>
</task>

<task type="auto">
  <name>Task 2: Integrate morning prompt into calendar page</name>
  <files>
    app/(app)/calendar/page.tsx
  </files>
  <action>
Update `app/(app)/calendar/page.tsx` to show the morning planning prompt.

1. Add state: `const [promptDismissed, setPromptDismissed] = useState(false)`

2. Fetch today's Big 3: `const todayStr = format(new Date(), "yyyy-MM-dd")` and `const { data: todayBig3 } = useTop3Todos(todayStr)`. Also fetch today's todos: `const { data: todayTodos } = useTodosByDate(todayStr)`.

3. Determine whether to show the prompt:
   - `const showPrompt = !promptDismissed && selectedDate is today (isSameDay) && todayBig3 is loaded && (todayBig3 as TodoListItem[]).length === 0`
   - Only show when viewing today, not when browsing other days.

4. If `showPrompt` is true, render `<MorningPlanningPrompt>` above the two-panel layout (between the page header and the grid/detail panels). Pass `todayTodos` (filtered to PENDING status only) and `onDismiss={() => setPromptDismissed(true)}`.

5. When Big 3 are successfully set, the `useTop3Todos` query will automatically refetch (because `useSetBig3` invalidates `queryKeys.todos.big3()`), causing `showPrompt` to become false and hiding the prompt naturally.

6. The `promptDismissed` state resets on page navigation (component unmount/remount), which is the desired behavior since the prompt should reappear on next visit if Big 3 are still not set.
  </action>
  <verify>
    `cd /Users/Shared/Domain/Code/Personal/goals && npx tsc --noEmit` passes with zero errors. Then `npm run build` succeeds.
  </verify>
  <done>Morning planning prompt appears when opening calendar with today selected and no Big 3 set. Setting Big 3 or dismissing hides the prompt. Prompt does not appear when browsing other days.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero TypeScript errors
2. `npm run build` succeeds with no compilation errors
3. Open /calendar with no Big 3 set for today and verify the planning prompt appears
4. Select 3 todos and click "Set Big 3" and verify prompt disappears and Big 3 section appears in day detail
5. Trying to select a 4th shows the enforcement toast
6. "Skip for now" dismisses the prompt without setting Big 3
</verification>

<success_criteria>
- Morning planning prompt appears when user opens calendar and today has no Big 3 set
- User can select up to 3 todos as Big 3, enforcement prevents selecting more
- Setting Big 3 dismisses the prompt and updates the day detail
- Skip button allows dismissing without action
- Prompt does not appear when browsing non-today dates
</success_criteria>

<output>
After completion, create `.planning/phases/14-calendar-view/14-02-SUMMARY.md`
</output>
