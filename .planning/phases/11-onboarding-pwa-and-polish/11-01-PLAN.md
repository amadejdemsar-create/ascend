---
phase: 11-onboarding-pwa-and-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/*/migration.sql
  - prisma/seed.ts
  - lib/services/dashboard-service.ts
  - app/api/goals/onboarding/route.ts
  - components/onboarding/onboarding-gate.tsx
  - components/onboarding/onboarding-choice.tsx
  - components/onboarding/onboarding-wizard.tsx
  - components/onboarding/onboarding-mcp-guide.tsx
  - components/onboarding/contextual-hints.tsx
  - components/dashboard/dashboard-page.tsx
autonomous: true
requirements:
  - ONBD-01
  - ONBD-02
  - ONBD-03
  - ONBD-04

must_haves:
  truths:
    - "First-time user sees a three-option choice: guided wizard, AI-guided setup, or skip"
    - "Guided wizard walks through creating a category, a yearly goal, and breaking it into a quarterly goal"
    - "AI-guided setup shows MCP connection instructions and detects when goals are created"
    - "Skip drops user into empty dashboard with contextual tooltip hints"
    - "Returning user with onboardingComplete=true never sees the onboarding flow"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "onboardingComplete field on User model"
      contains: "onboardingComplete"
    - path: "components/onboarding/onboarding-gate.tsx"
      provides: "Conditional render based on onboarding status"
      exports: ["OnboardingGate"]
    - path: "components/onboarding/onboarding-wizard.tsx"
      provides: "Multi-step wizard reusing GoalForm and category components"
      exports: ["OnboardingWizard"]
    - path: "components/onboarding/onboarding-mcp-guide.tsx"
      provides: "MCP setup instructions and goal-creation polling"
      exports: ["OnboardingMcpGuide"]
    - path: "app/api/goals/onboarding/route.ts"
      provides: "PATCH endpoint to mark onboarding complete"
      exports: ["PATCH"]
  key_links:
    - from: "components/dashboard/dashboard-page.tsx"
      to: "components/onboarding/onboarding-gate.tsx"
      via: "Conditional render when onboardingComplete is false"
      pattern: "onboardingComplete.*OnboardingGate"
    - from: "components/onboarding/onboarding-wizard.tsx"
      to: "/api/goals/onboarding"
      via: "PATCH call to mark onboarding complete after wizard finishes"
      pattern: "fetch.*onboarding"
---

<objective>
Implement the complete onboarding experience for first-time users with three paths: guided wizard, AI-guided MCP setup, and skip.

Purpose: New users need a structured entry point that helps them understand the goal hierarchy and get started quickly, while power users who want to configure via MCP or skip entirely can do so.

Output: Onboarding gate component, three-path flow (wizard, MCP guide, skip with hints), Prisma schema migration for onboardingComplete field, onboarding API endpoint.
</objective>

<execution_context>
@/Users/amadejdemsar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/amadejdemsar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-onboarding-pwa-and-polish/11-RESEARCH.md

<interfaces>
From lib/services/dashboard-service.ts:
```typescript
export type DashboardData = {
  weeklyFocus: (Goal & { category: Category | null })[];
  progressOverview: { categoryId: string | null; categoryName: string; color: string; completed: number; total: number }[];
  streaksStats: { totalGoals: number; completedThisMonth: number; completionRate: number; currentStreak: number; longestStreak: number; totalXp: number; level: number; weeklyScore: number };
  upcomingDeadlines: (Goal & { category: Category | null })[];
  onboardingComplete?: boolean;
};
```

From components/dashboard/dashboard-page.tsx:
```typescript
export function DashboardPage() {
  const { data, isLoading, isError, error, refetch } = useDashboard();
  // Renders loading, error, empty state ("Welcome to Ascend"), or widgets
}
```

From lib/auth.ts:
```typescript
export async function validateApiKey(request: NextRequest): Promise<{ success: true; userId: string } | { success: false }>;
```

From prisma/schema.prisma (User model):
```prisma
model User {
  id         String      @id @default(cuid())
  email      String?     @unique
  name       String?
  apiKey     String      @unique @default(cuid())
  createdAt  DateTime    @default(now())
  updatedAt  DateTime    @updatedAt
  goals      Goal[]
  categories Category[]
  stats      UserStats?
  xpEvents   XpEvent[]
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema migration, onboarding API, and dashboard data extension</name>
  <files>
    prisma/schema.prisma
    prisma/seed.ts
    lib/services/dashboard-service.ts
    app/api/goals/onboarding/route.ts
  </files>
  <action>
    1. Add `onboardingComplete Boolean @default(false)` to the User model in prisma/schema.prisma.

    2. Generate a migration using `npx prisma migrate diff --from-schema-datamodel prisma/schema.prisma --to-schema-datamodel prisma/schema.prisma --script` (following the project's established migration pattern from Phase 01-02). The migration should add the column with DEFAULT true for existing rows (so the seeded test user is treated as already onboarded) but the schema default is false for new users. Manually adjust the generated SQL: `ALTER TABLE "User" ADD COLUMN "onboardingComplete" BOOLEAN NOT NULL DEFAULT true;` for the migration, while the schema default remains `@default(false)`.

    3. Run `npx prisma generate` to regenerate the client.

    4. Update `dashboardService.getDashboardData` in lib/services/dashboard-service.ts to also fetch `onboardingComplete` from the User record and include it in the returned DashboardData. Add it to the DashboardData type. Fetch the user with `prisma.user.findUnique({ where: { id: userId }, select: { onboardingComplete: true } })` alongside the existing queries in the Promise.all batches.

    5. Create `app/api/goals/onboarding/route.ts` with a PATCH handler:
       - Authenticate via validateApiKey
       - Update the user: `prisma.user.update({ where: { id: auth.userId }, data: { onboardingComplete: true } })`
       - Return `{ success: true }`
       - Follow the thin route handler pattern (auth + service call + response)
  </action>
  <verify>
    cd /Users/Shared/Domain/Code/Personal/ascend/ && npx prisma generate && npx tsc --noEmit 2>&1 | head -20
  </verify>
  <done>
    User model has onboardingComplete field. Dashboard API includes onboardingComplete in response. PATCH /api/goals/onboarding marks user as onboarded. Migration SQL exists. TypeScript compiles without errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Onboarding gate, three-path choice, wizard, MCP guide, skip with hints</name>
  <files>
    components/onboarding/onboarding-gate.tsx
    components/onboarding/onboarding-choice.tsx
    components/onboarding/onboarding-wizard.tsx
    components/onboarding/onboarding-mcp-guide.tsx
    components/onboarding/contextual-hints.tsx
    components/dashboard/dashboard-page.tsx
  </files>
  <action>
    1. Create `components/onboarding/onboarding-gate.tsx`:
       - "use client" component that receives `onComplete: () => void` prop
       - Uses local state to track which path the user chose: "choice" | "wizard" | "mcp" | "skip"
       - Default state is "choice" (renders OnboardingChoice)
       - When a path is selected, renders the corresponding component
       - All paths eventually call `onComplete` which PATCHes /api/goals/onboarding and invalidates dashboard query

    2. Create `components/onboarding/onboarding-choice.tsx`:
       - Three cards in a centered layout:
         a) "Guided Setup" with a Wand2 icon: "Walk through creating your first goal step by step"
         b) "AI-Guided Setup" with a Bot icon: "Let your AI assistant set up goals through MCP"
         c) "Skip for Now" with a ArrowRight icon: "Jump into the empty dashboard"
       - Each card calls onSelect("wizard" | "mcp" | "skip")
       - Styled with the existing Card component and NativeAI palette colors

    3. Create `components/onboarding/onboarding-wizard.tsx`:
       - Multi-step wizard using local state (step index 0-3):
         Step 0: Welcome message explaining the yearly > quarterly > monthly > weekly hierarchy
         Step 1: Create a category (reuse the CategoryForm pattern from existing components/categories/category-form.tsx, simplified inline). Call POST /api/categories with hardcoded API_KEY.
         Step 2: Create a yearly goal in the category from step 1. Use GoalForm fields inline (title, description). Call POST /api/goals.
         Step 3: Create a quarterly sub-goal under the yearly goal. Show the parent context. Call POST /api/goals with parentId.
       - After step 3 completion, show a success screen with confetti (reuse canvas-confetti), then call onComplete
       - Navigation: "Back" and "Next"/"Create" buttons. Next is disabled until the step action is complete.
       - Use the existing API_KEY pattern from use-dashboard.ts: `process.env.NEXT_PUBLIC_API_KEY`

    4. Create `components/onboarding/onboarding-mcp-guide.tsx`:
       - Shows instructions for connecting MCP:
         a) "Add this to your Claude Code config:" with a code block showing the MCP server URL (ascend.nativeai.agency/api/mcp) and Authorization header
         b) "Ask your AI to create a yearly goal with sub-goals"
         c) A polling indicator that checks /api/dashboard every 5 seconds
       - When `data.streaksStats.totalGoals > 0`, show a "Setup Complete!" message with a "Continue to Dashboard" button that calls onComplete
       - Include a manual "I'm done" button that also calls onComplete (in case the user creates goals through MCP but polling misses it)

    5. Create `components/onboarding/contextual-hints.tsx`:
       - When skip path is chosen, show tooltip hints on the dashboard for first-time users
       - Three hints as small banners/cards at the top of the dashboard:
         a) "Use the sidebar to navigate between views"
         b) "Press N to quickly create a new goal"
         c) "Connect your AI assistant via MCP for guided goal setup"
       - Dismissible (localStorage flag "ascend-hints-dismissed")
       - Styled as subtle info cards with an X close button

    6. Update `components/dashboard/dashboard-page.tsx`:
       - After loading, check `data.onboardingComplete`
       - If `onboardingComplete === false`, render `<OnboardingGate onComplete={handleOnboardingComplete} />`
       - `handleOnboardingComplete` calls the PATCH endpoint, then invalidates the dashboard query (which will cause a refetch, and since onboardingComplete is now true, the dashboard renders normally)
       - Import OnboardingGate conditionally
       - Keep the existing "Welcome to Ascend" empty state for users who completed onboarding but have no goals
  </action>
  <verify>
    cd /Users/Shared/Domain/Code/Personal/ascend/ && npx tsc --noEmit 2>&1 | head -20
  </verify>
  <done>
    First-time user (onboardingComplete=false) sees three-path choice instead of dashboard. Guided wizard creates a category, yearly goal, and quarterly sub-goal through existing API endpoints. MCP guide shows connection instructions and polls for goal creation. Skip path shows contextual hints. All paths mark onboarding complete via API. Existing users are unaffected.
  </done>
</task>

</tasks>

<verification>
1. Dashboard API response includes `onboardingComplete` field
2. New user (onboardingComplete=false) sees OnboardingGate instead of dashboard widgets
3. Wizard path creates category and goals through existing API endpoints
4. MCP guide displays connection instructions with correct endpoint URL
5. Skip path renders dashboard with contextual hint banners
6. After any path completes, subsequent visits show the normal dashboard
7. TypeScript compiles without errors
</verification>

<success_criteria>
First-time users see a three-option onboarding screen. Each path (wizard, MCP, skip) leads to a functional starting state with onboardingComplete set to true. Returning users never see onboarding. The existing dashboard behavior is preserved for users who have completed onboarding.
</success_criteria>

<output>
After completion, create `.planning/phases/11-onboarding-pwa-and-polish/11-01-SUMMARY.md`
</output>
