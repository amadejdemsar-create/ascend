---
phase: 11-onboarding-pwa-and-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/manifest.ts
  - public/sw.js
  - public/icons/icon-192x192.png
  - public/icons/icon-512x512.png
  - public/icons/icon-maskable-512x512.png
  - components/pwa/sw-registration.tsx
  - components/pwa/install-prompt.tsx
  - app/layout.tsx
  - next.config.ts
autonomous: true
requirements:
  - PWA-01
  - PWA-02

must_haves:
  truths:
    - "App is installable from Chrome, Safari, and Edge with an install prompt"
    - "Installed PWA opens in standalone mode without browser chrome"
    - "Web app manifest serves correct metadata (name, icons, display, colors)"
    - "Service worker registers successfully on page load"
  artifacts:
    - path: "app/manifest.ts"
      provides: "Dynamic PWA manifest with MetadataRoute.Manifest type"
      exports: ["default"]
    - path: "public/sw.js"
      provides: "Service worker with install, activate, and fetch handlers"
    - path: "components/pwa/sw-registration.tsx"
      provides: "Client component that registers the service worker"
      exports: ["ServiceWorkerRegistration"]
    - path: "components/pwa/install-prompt.tsx"
      provides: "Install prompt banner for PWA installation"
      exports: ["InstallPrompt"]
    - path: "next.config.ts"
      provides: "Cache-Control headers for sw.js to prevent stale caching"
      contains: "sw.js"
  key_links:
    - from: "app/layout.tsx"
      to: "components/pwa/sw-registration.tsx"
      via: "ServiceWorkerRegistration component rendered in root layout"
      pattern: "ServiceWorkerRegistration"
    - from: "public/sw.js"
      to: "app/manifest.ts"
      via: "Manifest referenced in HTML head via Next.js metadata"
      pattern: "manifest"
---

<objective>
Make the app installable as a Progressive Web App with a web manifest, service worker, PWA icons, and install prompt.

Purpose: Users should be able to install Ascend on their device (phone, tablet, desktop) for quick access. The installed app opens in standalone mode without browser chrome, providing a native app feel.

Output: Dynamic manifest.ts, service worker (public/sw.js), PWA icons (192x192, 512x512, maskable), registration component, install prompt component, next.config.ts headers for sw.js cache control.
</objective>

<execution_context>
@/Users/amadejdemsar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/amadejdemsar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-onboarding-pwa-and-polish/11-RESEARCH.md

<interfaces>
From app/layout.tsx:
```typescript
export default function RootLayout({ children }: Readonly<{ children: React.ReactNode }>) {
  // html > body > ThemeProvider > QueryProvider > {children} + Toaster
}
```

From next.config.ts:
```typescript
const nextConfig: NextConfig = {
  output: "standalone",
  serverExternalPackages: ["@modelcontextprotocol/sdk", "pdfkit"],
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PWA manifest, icons, and next.config.ts headers</name>
  <files>
    app/manifest.ts
    public/icons/icon-192x192.png
    public/icons/icon-512x512.png
    public/icons/icon-maskable-512x512.png
    next.config.ts
  </files>
  <action>
    1. Create `app/manifest.ts` using Next.js MetadataRoute.Manifest type:
       - name: "Ascend"
       - short_name: "Ascend"
       - description: "Goal tracking that connects daily actions to yearly ambitions"
       - start_url: "/"
       - display: "standalone"
       - background_color: "#0F0F14" (dark bg from NativeAI palette)
       - theme_color: "#8B5CF6" (violet from NativeAI palette)
       - icons array with three entries: 192x192 (any), 512x512 (any), 512x512 maskable
       - All icon paths under /icons/

    2. Create PWA icons. Generate simple icons programmatically using a Node.js script that creates SVG files and converts them. The icon design: a solid circle with the NativeAI violet (#8B5CF6) background and a white upward arrow (mountain/ascend motif) in the center.
       - Create `public/icons/` directory
       - Generate SVGs and use the `sharp` package (install as devDependency) or use a simple canvas approach. Since we need actual PNG files, create a small Node.js generation script that:
         a) Creates an SVG string with the violet circle and white arrow
         b) Writes it as SVG files
         c) For the actual PNGs, use `npx @nicepkg/svg2png` or similar, OR simply create placeholder PNGs that are valid images.
       - Alternative (simpler): Create the icons using Puppeteer/headless Chrome to render an HTML page to screenshot, OR just create valid PNG files using raw bytes for a solid violet square with white center. The simplest approach: use Node.js `canvas` or just create minimal valid PNG data.
       - **Recommended approach:** Create a tiny Node.js script (`scripts/generate-icons.js`) that uses `pdfkit` (already in dependencies) or the built-in `node:fs` to create minimal valid PNG files. Actually, the simplest: use `npx create-pwa-icons` or write raw PNG binary. Since the project already has `pdfkit`, create a script that generates the icons. BUT the very simplest approach that works: write an inline SVG as the icon source, and for the actual PNGs, use `npx sharp-cli` or just create them with a script.

       **Final approach (simplest):** Write a small `scripts/generate-pwa-icons.mjs` that creates SVG content and uses the `sharp` npm package (add as devDependency) to convert SVG to PNG at 192x192 and 512x512. The SVG is a rounded rectangle (#8B5CF6) with a white upward chevron/arrow. Run it once during this task, then remove the script dependency if desired. The generated PNGs are committed to the repo.

    3. Update `next.config.ts` to add headers for sw.js:
       - Add `async headers()` function returning Cache-Control: no-cache, no-store, must-revalidate for /sw.js source
       - Add Content-Type: application/javascript; charset=utf-8 for /sw.js
       - Keep existing `output: "standalone"` and `serverExternalPackages`
  </action>
  <verify>
    cd /Users/Shared/Domain/Code/Personal/goals/ && npx tsc --noEmit 2>&1 | head -10 && test -f public/icons/icon-192x192.png && echo "192 icon exists" && test -f public/icons/icon-512x512.png && echo "512 icon exists"
  </verify>
  <done>
    manifest.ts exports a valid MetadataRoute.Manifest. Three PNG icons exist in public/icons/. next.config.ts includes sw.js cache headers. TypeScript compiles without errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Service worker, registration component, and install prompt</name>
  <files>
    public/sw.js
    components/pwa/sw-registration.tsx
    components/pwa/install-prompt.tsx
    app/layout.tsx
  </files>
  <action>
    1. Create `public/sw.js` (plain JavaScript, not TypeScript):
       - Define CACHE_NAME = 'ascend-v1'
       - Install event: `self.skipWaiting()` (no precaching needed yet, offline cache comes in Plan 03)
       - Activate event: Clean old caches (delete any cache not matching CACHE_NAME), call `self.clients.claim()`
       - Fetch event: For now, just pass through all requests (offline caching strategy added in Plan 03). This establishes the service worker lifecycle without offline complexity.
       - Keep the service worker minimal. Plan 03 adds the actual caching strategies.

    2. Create `components/pwa/sw-registration.tsx`:
       - "use client" component
       - In useEffect, check `'serviceWorker' in navigator`
       - Register `/sw.js` with `{ scope: '/', updateViaCache: 'none' }`
       - Log registration success/failure to console
       - Return null (renders nothing)

    3. Create `components/pwa/install-prompt.tsx`:
       - "use client" component
       - Listen for the `beforeinstallprompt` event (Chrome/Edge)
       - Store the event in a ref
       - Show a subtle banner at the bottom of the screen (above the mobile tab bar) with "Install Ascend" and an install button
       - Clicking the button calls `event.prompt()` and awaits the user's choice
       - If already installed (standalone display mode detected via `window.matchMedia('(display-mode: standalone)')`), render nothing
       - Dismissible with a close button; store dismissal in localStorage ("ascend-install-dismissed")
       - Styled with existing Card/Button components, subtle background

    4. Update `app/layout.tsx`:
       - Import and render `<ServiceWorkerRegistration />` inside the body, after the Toaster
       - Import and render `<InstallPrompt />` after ServiceWorkerRegistration
       - Both are client components that return null or a portal, so they work inside the server layout via composition
  </action>
  <verify>
    cd /Users/Shared/Domain/Code/Personal/goals/ && npx tsc --noEmit 2>&1 | head -10 && test -f public/sw.js && echo "sw.js exists"
  </verify>
  <done>
    Service worker registers on page load. Install prompt appears for eligible browsers. App is installable and opens in standalone mode. Root layout includes both PWA components. Service worker handles install/activate lifecycle (caching strategies deferred to Plan 03).
  </done>
</task>

</tasks>

<verification>
1. `app/manifest.ts` exports valid manifest with correct name, icons, and display: standalone
2. `public/sw.js` exists and handles install/activate/fetch events
3. `next.config.ts` includes Cache-Control headers for sw.js
4. Three PWA icons exist in public/icons/ at correct dimensions
5. ServiceWorkerRegistration component is rendered in root layout
6. InstallPrompt component is rendered in root layout
7. TypeScript compiles without errors
</verification>

<success_criteria>
The app serves a valid web manifest, registers a service worker, displays PWA icons, and shows an install prompt on eligible browsers. Installed PWA opens in standalone mode without browser chrome.
</success_criteria>

<output>
After completion, create `.planning/phases/11-onboarding-pwa-and-polish/11-02-SUMMARY.md`
</output>
