---
phase: 15-dashboard-transformation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/dashboard/todays-big3-widget.tsx
  - components/dashboard/dashboard-page.tsx
autonomous: true
requirements: [VS-03, VS-04]

must_haves:
  truths:
    - "Dashboard prominently displays Today's Big 3 widget as the first and largest element"
    - "Each Big 3 to-do shows its linked goal (output), making visible why this input matters"
    - "Existing dashboard widgets (streaks, deadlines, progress) remain functional"
  artifacts:
    - path: "components/dashboard/todays-big3-widget.tsx"
      provides: "Today's Big 3 widget component"
      min_lines: 40
    - path: "components/dashboard/dashboard-page.tsx"
      provides: "Restructured input-centric dashboard layout"
      contains: "TodaysBig3Widget"
  key_links:
    - from: "components/dashboard/todays-big3-widget.tsx"
      to: "/api/todos/big3"
      via: "useTop3Todos hook"
      pattern: "useTop3Todos"
    - from: "components/dashboard/todays-big3-widget.tsx"
      to: "todo.goal"
      via: "renders linked goal title per Big 3 item"
      pattern: "goal.*title"
    - from: "components/dashboard/dashboard-page.tsx"
      to: "components/dashboard/todays-big3-widget.tsx"
      via: "import and render as first widget"
      pattern: "TodaysBig3Widget"
---

<objective>
Transform the dashboard from output-centric (goals only) to input-centric by adding a prominent Today's Big 3 widget and restructuring the layout so inputs come first.

Purpose: The core philosophy of Ascend v2.0 is "focus on inputs and the outputs will come." The dashboard should immediately answer "what are my 3 most important tasks today?" with clear connections to why each matters (linked goals).

Output: A TodaysBig3Widget component and a restructured dashboard-page.tsx with the Big 3 widget spanning full width at the top, followed by existing widgets below.
</objective>

<execution_context>
@/Users/amadejdemsar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/amadejdemsar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@components/dashboard/dashboard-page.tsx
@components/dashboard/weekly-focus-widget.tsx
@components/calendar/calendar-day-detail.tsx
@lib/hooks/use-todos.ts
@lib/services/todo-service.ts
@components/todos/todo-list-columns.tsx

<interfaces>
<!-- Key types and hooks the executor needs -->

From lib/hooks/use-todos.ts:
```typescript
export function useTop3Todos(date?: string);  // Returns today's Big 3 via /api/todos/big3
export function useCompleteTodo();             // Mutation to toggle todo completion
```

From components/todos/todo-list-columns.tsx:
```typescript
export interface TodoListItem {
  id: string;
  title: string;
  description: string | null;
  status: "PENDING" | "DONE" | "SKIPPED";
  priority: "LOW" | "MEDIUM" | "HIGH";
  dueDate: string | null;
  scheduledDate: string | null;
  isBig3: boolean;
  isRecurring: boolean;
  currentStreak: number;
  completedAt: string | null;
  createdAt: string;
  goal: { id: string; title: string } | null;
  category: { id: string; name: string; color: string; icon: string | null } | null;
}
```

From lib/services/dashboard-service.ts:
```typescript
export interface DashboardData {
  weeklyFocus: WeeklyFocusGoal[];
  progressOverview: CategoryProgress[];
  streaksStats: StatsData;
  upcomingDeadlines: DeadlineGoal[];
  onboardingComplete: boolean;
}
```

The Big 3 API (GET /api/todos/big3) returns TodoListItem[] with `goal: { id, title }` included when a todo is linked to a goal.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TodaysBig3Widget component</name>
  <files>components/dashboard/todays-big3-widget.tsx</files>
  <action>
Create a new "use client" component TodaysBig3Widget that:

1. Calls `useTop3Todos()` (no date arg = defaults to today) and `useCompleteTodo()` from `@/lib/hooks/use-todos`.
2. Casts the result as `TodoListItem[]` from `@/components/todos/todo-list-columns`.
3. Renders inside a Card (from @/components/ui/card) with:
   - CardHeader with a Star icon (lucide) and title "Today's Big 3"
   - CardContent with up to 3 items

4. Each Big 3 item renders as a row with:
   - A checkbox button (circle outline when pending, filled check when done) that calls completeTodo.mutate({ id, completed: status !== "DONE" })
   - Title text (line-through + muted when done)
   - Priority badge using GoalPriorityBadge from @/components/goals/goal-priority-badge
   - **VS-04: Linked output display** — if `todo.goal` is not null, render a small secondary line below the title: an ArrowRight icon (lucide, size-3) followed by `todo.goal.title` in text-xs text-muted-foreground. This is the "why this input matters" connection.

5. Empty state: if big3 is empty (length 0), show a message "No Big 3 set for today" with a link/button labeled "Set priorities" that navigates to /calendar (where the morning planning prompt lives). Use next/link.

6. Loading state: show 3 Skeleton rows while loading.

Style notes:
- Use the same card styling patterns as WeeklyFocusWidget (Card/CardHeader/CardTitle/CardContent).
- The widget should feel larger and more prominent than the other widgets (it will span full width via the parent grid).
- Category color dot before the title (same pattern as WeeklyFocusWidget: a small colored circle if category exists).
  </action>
  <verify>
    <automated>cd /Users/Shared/Domain/Code/Personal/goals && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>TodaysBig3Widget component exists, renders Big 3 todos with linked goal names visible, handles empty/loading states, and passes TypeScript compilation.</done>
</task>

<task type="auto">
  <name>Task 2: Restructure dashboard layout to input-centric</name>
  <files>components/dashboard/dashboard-page.tsx</files>
  <action>
Modify the existing dashboard-page.tsx to make it input-centric:

1. Import TodaysBig3Widget from "./todays-big3-widget".

2. Update the header subtitle from "What should you focus on right now?" to "What are your inputs today?" to reflect the inputs/outputs philosophy.

3. In the Quick Actions bar, add a "New To-do" button (PlusIcon + "New To-do") as the first button. It should navigate to /todos (the todo list page where quick-add exists). Use next/link wrapped in Button variant="outline" size="sm". Keep the existing "New Weekly Goal" and "New Yearly Goal" buttons after it.

4. Restructure the widget grid (the section currently showing 4 widgets in a 2-col grid):
   - BEFORE the existing 2-col grid, render `<TodaysBig3Widget />` at full width (no grid, just a standalone component). This makes it the first and largest visual element after the header.
   - Add a small spacing div (mb-4) between the Big 3 widget and the existing grid.
   - Keep the existing grid with WeeklyFocusWidget, ProgressOverviewWidget, StreaksStatsWidget, and UpcomingDeadlinesWidget exactly as they are.

5. The Big 3 widget must also appear inside the conditional that checks `data.streaksStats.totalGoals > 0` (same as the other widgets) so it does not show on the empty welcome state. However, it should ALSO show when totalGoals is 0 but there are todos. Since Big 3 is self-contained (fetches its own data via useTop3Todos), place it just above the existing grid but still inside the data-loaded conditional block.

Do NOT modify any of the existing widget components (WeeklyFocusWidget, ProgressOverviewWidget, StreaksStatsWidget, UpcomingDeadlinesWidget). They remain untouched.
  </action>
  <verify>
    <automated>cd /Users/Shared/Domain/Code/Personal/goals && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>Dashboard renders TodaysBig3Widget as the first and most prominent element. Existing widgets remain below in the 2-col grid, fully functional. The subtitle reflects input-centric framing.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no errors
2. Dashboard page loads at /dashboard showing:
   - "Today's Big 3" widget spanning full width at top
   - Each Big 3 todo shows its linked goal name (or no link indicator if unlinked)
   - Completing a Big 3 todo via checkbox toggles it
   - Empty state shows "Set priorities" link to /calendar
   - Existing 4 widgets (Weekly Focus, Progress Overview, Streaks, Deadlines) render below in 2-col grid
</verification>

<success_criteria>
- VS-03: Dashboard is input-centric with Big 3 as the first and largest widget
- VS-04: Each Big 3 item displays its linked goal (output), showing why the input matters
- All existing dashboard widgets remain functional and visually unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/15-dashboard-transformation/15-01-SUMMARY.md`
</output>
