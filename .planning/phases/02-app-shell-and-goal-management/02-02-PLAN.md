---
phase: 02-app-shell-and-goal-management
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - app/(app)/layout.tsx
  - app/(app)/page.tsx
  - app/(app)/goals/page.tsx
  - app/(app)/settings/page.tsx
  - components/layout/app-sidebar.tsx
  - components/layout/bottom-tab-bar.tsx
  - components/layout/mobile-drawer.tsx
  - components/layout/nav-config.ts
  - components/layout/theme-toggle.tsx
autonomous: true
requirements: [NAV-01, NAV-02, NAV-03, NAV-04, NAV-05]

must_haves:
  truths:
    - "User sees a collapsible sidebar on desktop (full sidebar or icons only)"
    - "Sidebar shows navigation (Dashboard, Goals), a categories placeholder section, and Settings link"
    - "User sees a bottom tab bar on mobile with Dashboard, Goals, and Settings tabs"
    - "Mobile has a hamburger/menu button that opens a drawer with secondary navigation"
    - "Layout responds to three breakpoints: mobile (<768px), tablet (768-1024px), desktop (>1024px)"
  artifacts:
    - path: "app/(app)/layout.tsx"
      provides: "App shell layout with SidebarProvider, AppSidebar, SidebarInset, BottomTabBar"
      contains: "SidebarProvider"
    - path: "components/layout/app-sidebar.tsx"
      provides: "Desktop sidebar with navigation groups, categories placeholder, settings"
      exports: ["AppSidebar"]
    - path: "components/layout/bottom-tab-bar.tsx"
      provides: "Fixed bottom navigation for mobile with Dashboard, Goals, Settings tabs"
      exports: ["BottomTabBar"]
    - path: "components/layout/mobile-drawer.tsx"
      provides: "Sheet-based drawer for secondary mobile navigation"
      exports: ["MobileDrawer"]
    - path: "components/layout/theme-toggle.tsx"
      provides: "Dark/light mode toggle button using next-themes"
      exports: ["ThemeToggle"]
    - path: "components/layout/nav-config.ts"
      provides: "Navigation items configuration (labels, icons, hrefs)"
      exports: ["mainNavItems", "secondaryNavItems"]
  key_links:
    - from: "app/(app)/layout.tsx"
      to: "components/layout/app-sidebar.tsx"
      via: "import and render inside SidebarProvider"
      pattern: "AppSidebar"
    - from: "app/(app)/layout.tsx"
      to: "components/layout/bottom-tab-bar.tsx"
      via: "import and render after SidebarInset"
      pattern: "BottomTabBar"
    - from: "components/layout/app-sidebar.tsx"
      to: "components/layout/nav-config.ts"
      via: "import nav items array and map to SidebarMenuItems"
      pattern: "mainNavItems"
---

<objective>
Build the responsive app shell with a collapsible desktop sidebar (using shadcn/ui Sidebar), a mobile bottom tab bar, and a mobile hamburger drawer. Create the route group layout that wraps all app pages in this shell.

Purpose: Users need to navigate between Dashboard, Goals, and Settings. The shell provides the persistent navigation frame that all content pages render inside. Desktop users get a sidebar; mobile users get a bottom tab bar with a drawer for secondary items.

Output: Functional app shell with responsive navigation that adapts between desktop sidebar and mobile bottom tabs. Three placeholder pages (Dashboard, Goals, Settings) render inside the shell.
</objective>

<execution_context>
@/Users/amadejdemsar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/amadejdemsar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-app-shell-and-goal-management/02-RESEARCH.md
@.planning/phases/02-app-shell-and-goal-management/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (must exist before this runs) -->

From components/providers/theme-provider.tsx:
```typescript
export function ThemeProvider({ children, ...props }: ThemeProviderProps): JSX.Element;
```

From lib/stores/ui-store.ts:
```typescript
export const useUIStore: UseBoundStore<StoreApi<UIStore>>;
// UIStore includes: sidebarCollapsed, toggleSidebar, selectedGoalId, selectGoal, goalModalOpen, openGoalModal, closeGoalModal
```

From components/ui/sidebar.tsx (shadcn/ui):
```typescript
// shadcn/ui Sidebar exports: Sidebar, SidebarContent, SidebarGroup, SidebarGroupLabel,
// SidebarGroupContent, SidebarMenu, SidebarMenuItem, SidebarMenuButton,
// SidebarProvider, SidebarInset, SidebarTrigger, SidebarFooter, SidebarHeader,
// SidebarRail, useSidebar
```

From components/ui/sheet.tsx (shadcn/ui):
```typescript
// Sheet, SheetContent, SheetTrigger, SheetHeader, SheetTitle, SheetDescription
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create navigation config and layout components</name>
  <files>components/layout/nav-config.ts, components/layout/theme-toggle.tsx, components/layout/bottom-tab-bar.tsx, components/layout/mobile-drawer.tsx</files>
  <action>
    **1. Create components/layout/nav-config.ts** (plain TypeScript, not a component):
    - Define a `NavItem` type: `{ label: string; href: string; icon: LucideIcon }`.
    - Export `mainNavItems`: Dashboard (LayoutDashboard icon, href "/"), Goals (Target icon, href "/goals").
    - Export `secondaryNavItems`: Settings (Settings icon, href "/settings").
    - These arrays drive both the sidebar and bottom tab bar to keep navigation consistent.

    **2. Create components/layout/theme-toggle.tsx** ("use client"):
    - Import `useTheme` from next-themes.
    - Render a shadcn/ui Button (variant="ghost", size="icon") that cycles between light/dark/system.
    - Show Sun icon for light, Moon icon for dark, Monitor icon for system.
    - Use `setTheme` to cycle: if current is "light" set "dark", if "dark" set "system", if "system" set "light".
    - Handle mounted state: return a Skeleton placeholder until mounted to avoid hydration mismatch.

    **3. Create components/layout/bottom-tab-bar.tsx** ("use client"):
    - Fixed to bottom of screen, only visible on mobile (`md:hidden`).
    - Render mainNavItems + secondaryNavItems as tab buttons.
    - Highlight active tab based on `usePathname()`.
    - Include a menu button (Menu icon from lucide-react) that triggers the MobileDrawer via a Sheet.
    - Style: `fixed bottom-0 left-0 right-0 z-50 border-t border-border bg-background`.
    - Tab bar height: h-16. Each tab: flex column, icon + label, text-xs.
    - Active tab: `text-primary`. Inactive: `text-muted-foreground`.

    **4. Create components/layout/mobile-drawer.tsx** ("use client"):
    - Uses shadcn/ui Sheet component (side="left").
    - Contains secondary navigation: categories (placeholder text "Categories coming in Phase 3"), archive link (placeholder), and the ThemeToggle.
    - Accepts `open` and `onOpenChange` props so BottomTabBar can control it.
  </action>
  <verify>
    Run `npx tsc --noEmit` to confirm TypeScript compilation.
    Verify files exist: `ls components/layout/nav-config.ts components/layout/theme-toggle.tsx components/layout/bottom-tab-bar.tsx components/layout/mobile-drawer.tsx`
  </verify>
  <done>Navigation config defines consistent nav items. ThemeToggle cycles light/dark/system. BottomTabBar shows fixed mobile navigation. MobileDrawer provides sheet-based secondary navigation.</done>
</task>

<task type="auto">
  <name>Task 2: Create app sidebar, route group layout, and placeholder pages</name>
  <files>components/layout/app-sidebar.tsx, app/(app)/layout.tsx, app/(app)/page.tsx, app/(app)/goals/page.tsx, app/(app)/settings/page.tsx</files>
  <action>
    **1. Create components/layout/app-sidebar.tsx** ("use client"):
    - Use shadcn/ui Sidebar component with `collapsible="icon"` prop for icons-only collapse mode.
    - SidebarHeader: App logo/title "Ascend" with a small Target icon. In collapsed mode, show only the icon.
    - SidebarContent with three SidebarGroups:
      a. **Navigation group**: Map `mainNavItems` to SidebarMenuItem + SidebarMenuButton. Each button wraps a Link component. Use `usePathname()` for active state (`isActive` prop on SidebarMenuButton).
      b. **Categories group** (label: "Categories"): Placeholder content "Coming in Phase 3" as muted text. This section will be populated in Phase 3.
      c. **Settings group**: Map `secondaryNavItems` similarly.
    - SidebarFooter: Render the ThemeToggle component.
    - SidebarRail: Include `<SidebarRail />` to enable the drag-to-resize rail.
    - Hidden on mobile: The shadcn/ui Sidebar component handles this automatically via its responsive behavior.

    **2. Move existing app/page.tsx to app/(app)/page.tsx** as the dashboard placeholder:
    - The existing page.tsx is a simple placeholder. Move it into the route group.
    - Update it to show a simple heading: "Dashboard" with `font-serif text-3xl font-bold` (using Playfair Display).
    - Add subtitle: "What should you focus on right now?" in muted text.
    - Add a note: "Dashboard widgets coming in Phase 4."

    **3. Create app/(app)/goals/page.tsx** as the goals page placeholder:
    - Server Component.
    - Heading: "Goals" with `font-serif text-3xl font-bold`.
    - Subtitle: "Track your ambitions from yearly to weekly."
    - Note: "Goal management UI will be added in plans 02-03 and 02-04."

    **4. Create app/(app)/settings/page.tsx** as the settings placeholder:
    - Heading: "Settings" with `font-serif text-3xl font-bold`.
    - Note: "Settings coming in later phases."

    **5. Create app/(app)/layout.tsx** (Server Component):
    - Import SidebarProvider and SidebarInset from shadcn/ui sidebar.
    - Import AppSidebar and BottomTabBar.
    - Structure:
      ```
      <SidebarProvider>
        <AppSidebar />
        <SidebarInset>
          <main className="flex-1 p-4 md:p-6 pb-20 md:pb-6">
            {children}
          </main>
        </SidebarInset>
        <BottomTabBar />
      </SidebarProvider>
      ```
    - The `pb-20` on main ensures content is not hidden behind the mobile bottom tab bar. `md:pb-6` restores normal padding on desktop.

    **6. Update the root app/page.tsx** (if it still exists after moving):
    - The root `app/page.tsx` should redirect to `app/(app)/page.tsx` or be removed. Since `(app)` is a route group (parentheses), the route `app/(app)/page.tsx` serves the `/` path. So simply delete the old `app/page.tsx` if it exists outside the route group. The route group page replaces it.
  </action>
  <verify>
    Run `npx tsc --noEmit` to confirm TypeScript compilation.
    Run `npm run build` to confirm production build succeeds.
    Verify route group structure: `ls app/\(app\)/layout.tsx app/\(app\)/page.tsx app/\(app\)/goals/page.tsx app/\(app\)/settings/page.tsx`
  </verify>
  <done>App shell renders with collapsible sidebar on desktop (full/icons-only), bottom tab bar on mobile, working navigation between Dashboard (/), Goals (/goals), and Settings (/settings). Theme toggle in sidebar footer. All three pages show placeholder content inside the shell. Responsive breakpoints work: sidebar hidden on mobile, bottom tabs hidden on desktop.</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds
2. `npx tsc --noEmit` passes
3. Desktop: sidebar visible with Dashboard, Goals links and collapsed/expanded toggle
4. Mobile: bottom tab bar visible, sidebar hidden
5. Navigation between /, /goals, /settings works
6. Theme toggle cycles between light, dark, system
7. Content has proper bottom padding on mobile (not hidden behind tab bar)
</verification>

<success_criteria>
- Sidebar collapses from full to icons-only on desktop (via SidebarRail or trigger)
- Bottom tab bar shows on screens < 768px with Dashboard, Goals, Settings, Menu tabs
- Menu tab opens the mobile drawer Sheet
- Active navigation item is highlighted with primary color
- Three placeholder pages render inside the shell
- Theme toggle is accessible in sidebar footer (desktop) and mobile drawer
- Responsive breakpoints: mobile (<768px), tablet (768-1024px), desktop (>1024px)
</success_criteria>

<output>
After completion, create `.planning/phases/02-app-shell-and-goal-management/02-02-SUMMARY.md`
</output>
