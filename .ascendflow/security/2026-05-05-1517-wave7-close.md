ASCEND SECURITY AUDIT
=====================

Audit date: 5. 5. 2026
Auth mode: MIXED (API key + JWT cookie + JWT Bearer)
Scope: Wave 7 (Provenance + Time Travel) close-out, 12 commits

Security checks (global):
  S1 through S5: PASS
  S6 through S13: N/A or NOTE (rate limiting is the only NOTE)

Per-item Wave 7 checks: 17/17 PASS

VERDICT: AUDIT PASS WITH NOTES

Notes:
1. restore-service.ts re-throws downstream errors with the original message; if a Prisma error bubbles up, schema field names could leak to the client. Non-critical but worth wrapping for hardening.
2. Rate limiting absent on the 5 user-facing routes; required by Wave 8 (multi-user).
3. graph-snapshot replay loads all EdgeEvents up to cutoff into memory; cron-secret gate mitigates DoS adequately for now.

Summary: Wave 7 passes all security checks. Every Prisma query userId-scoped, all routes authenticate or cron-secret, cross-user attack surface verified blocked, restore writes forward snapshot before mutate, branch cycle detection caps at 100 hops, retention DELETE includes userId guard, MCP tools take userId from server factory, no secret leaks. Ready to close.
