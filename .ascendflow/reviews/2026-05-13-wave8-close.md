# Wave 8 Close-Out Review — `ascend-reviewer`

**Date:** 13. 5. 2026
**Scope:** Cumulative review across Wave 8 Phases 1 through 9 (11 commits on `main`).
**Final verdict:** PASS (after must-fix `ea2ba04` landed).

## Commit range audited

| SHA | Subject |
|---|---|
| `75abf6e` | Phase 1 — workspace primitives schema + 2 migrations |
| `baf7ef8` | Phase 2 — workspaceId on every entity, backfill, NOT NULL flip |
| `006a8ec` | Phase 3a — workspace plumbing services + auth flow extension |
| `f7d05e8` | Phase 3b — mass refactor to consume workspaceId auth context |
| `cf72972` | Phase 4 — Hocuspocus server + token/persist endpoints |
| `408f456` | Phase 4 hotfix — wget healthcheck replaced with node http call |
| `c684c35` | Phase 4 hotfix — copy pnpm .pnpm store to runner |
| `fb43f3e` | Phase 5 — Lexical client binding to Hocuspocus |
| `fd0a136` | Phase 6 — presence avatars + collaborative cursors |
| `eda338b` | Phase 7 — activity feed |
| `698afc9` | Phase 8 — workspace switcher + settings page |
| `5c2f375` | Phase 9 — 3 workspace MCP tools (73 → 76) |
| `ea2ba04` | Wave 8 close must-fixes (this audit's findings) |

## Rule checks

| Rule | Verdict | Evidence |
|------|---------|----------|
| Rule 1 (userId + workspaceId in every Prisma where) | PASS (after `ea2ba04`) | Pre-fix FAIL on `branch-service.ts` lines 112 + 238; fix added workspaceId to `_cycleCheck` and the derivative count. All other touched services audited end-to-end. |
| Rule 2 (Zod validation in routes) | PASS | All new Wave 8 routes Zod-validate via schemas from `@ascend/core`. |
| Rule 3 (service layer is the only Prisma consumer) | PASS | Zero `@/lib/db` imports in routes / components. |
| Rule 4 (cache invalidation on mutations) | PASS | 6 mutation hook files invalidate `queryKeys.activity.all()` per Phase 7 wiring. |
| Rule 5 (web build passes) | PASS | `pnpm --filter @ascend/web build` clean. |
| Rule 6 (no `prisma db push` / `migrate reset`) | PASS | All 4 Wave 8 migrations hand-written, additive. |

## Danger zones

- DZ-2 (`search_vector`) — PASS. All four Wave 8 migrations include explicit DZ-2 SAFE headers; cumulative SQL review by `ascend-migration-auditor` confirms search_vector untouched.
- DZ-8 (ContextLink.userId denormalization) — PASS. Extended to `workspaceId`; every link query includes both.
- DZ-10 (BlockDocument 1 MiB cap) — PASS. `persistFromCrdt` decodes base64 then size-checks raw bytes.
- DZ-21 / DZ-22 / DZ-23 / DZ-24 — Documented in CLAUDE.md. Mitigations implemented in code per the matrix.

## Pattern compliance

All new services follow the const-object pattern. All new API routes follow authenticate -> parse -> service -> respond. All MCP handlers use the canonical `McpContent` shape with separate `ZodError` catch and factory-bound `(userId, workspaceId)`.

## Blocking issues (pre-fix)

1. **`branch-service.ts:112, :238`** — missing `workspaceId` in `_cycleCheck` and derivative count queries. **RESOLVED in `ea2ba04`**: added `workspaceId` parameter, threaded it through the call chain, added the filter to both Prisma calls. Pattern verified clean post-fix.

## Non-blocking notes

- `useUpdateGoal` does not invalidate `queryKeys.activity.all()`; this is correct since `goalService.update` does not log an activity event. Goal completion (`completeWithSideEffects`) also does not log; consider whether NODE_UPDATED (or a completion-specific event) should fire as a Wave 8b enhancement.
- Gamification service stays user-scoped (no workspaceId) as XP/levels/streaks are global to the user. Comments at `goalService.completeWithSideEffects:183` and `todoService.complete:207` explicitly document this design decision.

## Audit scope

Read end-to-end: 8 new services (workspaceService, workspaceMembershipService, workspaceContextService, permissionService, activityEventService, plus Phase 4-5 CRDT services), 4 new routes under `/api/workspaces`, 3 MCP tool handlers, the entire `apps/crdt/` source tree (4 files), `auth.ts`, `block-document-service.ts`, `branch-service.ts`, `restore-service.ts`, `context-link-service.ts`, `category-service.ts`, all 6 modified hooks, query keys, 4 migration SQL files, 3 new component sets (activity, workspace, realtime), the MCP server dispatcher. Spot-checked via grep: every modified route file for `auth.workspaceId` usage, all files for `@/lib/db` or `@prisma/client` imports outside services, all files for `search_vector` references.

## Final verdict

**PASS.** Single blocking issue resolved inline. Wave 8 close gate satisfied.
