# Ascend Review: Wave 5 (Databases + properties) Close

**Date**: 4. 5. 2026 13:47 Europe/Ljubljana
**Branch**: main (13 commits ahead of origin at audit time, 14 after this commit)
**Scope**: `git diff origin/main..HEAD` cumulative wave diff (~109 files, ~18,000 insertions)

**TypeScript**: PASS (zero errors at every commit)
**Build**: PASS (10.2s, 62 routes most recent)
**Initial verdict**: FAIL (3 blocking issues)
**Post-fix verdict**: PASS WITH NOTES

## Safety Rule Checks

| Rule | Initial | Post-fix |
|------|---------|----------|
| 1. userId in every Prisma where clause | FAIL (1 raw SQL DELETE missing the guard) | PASS |
| 2. Zod validation in API routes | PASS | PASS |
| 3. Service layer is the only Prisma importer | PASS | PASS |
| 4. Cache invalidation on mutations | PASS | PASS |
| 5. npm run build passes | PASS | PASS |
| 6. No forbidden Prisma commands | PASS | PASS |

## Pattern Compliance

| Pattern | Status |
|---------|--------|
| Service patterns | PASS |
| API route patterns | PASS |
| Component patterns | PASS |
| MCP tool patterns | FAIL initial (1 handler wrapped properties wrong); PASS post-fix |

## Danger Zones

| DZ | Status |
|----|--------|
| DZ-2 (search_vector) | PASS — migration #4 is intentional no-op; trigger and GIN index untouched |
| DZ-7 (no error boundaries) | PASS — every database view has its own boundary |
| DZ-8 (ContextLink.userId) | PASS post-fix (was FAIL on diffAndApply) |
| DZ-14 (Formula CPU/memory) | PASS — all 5 mitigations enforced (1000 node parse cap, 10k ops, 50ms timeout, depth 50, cycle detection) |
| DZ-15 (JSONB property bloat) | PASS — per-property Zod caps, 256 KiB service pre-flight, 512 KiB DB CHECK |
| DZ-16 (RELATION cascade) | PASS post-fix — all 3 raw SQL DELETEs include userId guard |

## Initial Findings (Blocking, all fixed before close)

1. **`use-database-rows.ts:201`** — Hook sent `{ properties }` but route expected `{ propertiesPatch }`. Would have 400'd every cell edit. Fixed.
2. **`mcp/tools/database-tools.ts:160-162`** — `create_row` wrapped properties in extra `{ properties }` object instead of passing the raw map. Would have broken AI row creation. Fixed.
3. **`database-relation-service.ts:66-71`** — Raw SQL DELETE missing `AND "userId" = ${userId}` guard. DZ-16 defense-in-depth gap. Fixed.

## Non-Blocking Notes (deferred to BACKLOG)

- `databaseFieldService.cycleCheck` does not include `userId` in its query (called only after upstream ownership verification). Defense-in-depth gap.
- 3 routes (`by-entry`, `relation-backlinks`, `row-by-entry`) import `validateApiKey` while others use `authenticate` (alias). Standardize.
- `databaseQueryService.query` falls back to in-memory filter+sort for FORMULA-targeted clauses, capped at 10k rows. Perf cliff at scale.

## Summary

Wave 5 ships ~18,000 LOC across 109 files. Architecture is sound: schema, migrations, formula engine (1890 LOC, 28/28 fixtures), 6 services, 13 routes (17 handlers), 5 hook files, 14 property editors with Cell + Expanded modes, 5 view types with full DnD interactions, filter+sort+view config builder, detail integration, slash menu wiring, 10 MCP tools (count 58 → 68). All 6 safety rules verified post-fix. All documented danger zones (existing DZ-2, DZ-7, DZ-8 + new DZ-14, DZ-15, DZ-16) properly mitigated. The 3 blocking issues caught by this review were all field-name mismatches that TypeScript could not catch because `Record<string, unknown>` is too permissive at the relevant call sites — exactly the class of error a cumulative reviewer pass exists to catch. All 3 are now fixed in the wave-close commit.

**Verdict (post-fix): PASS WITH NOTES.** Wave may close.
